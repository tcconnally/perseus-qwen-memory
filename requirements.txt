# stdlib only — no dependencies
# The agent uses Python standard library for HTTP and JSON.
# For standalone Elasticsearch mode, install: pip install elasticsearch

# Shared memory + tool layer (extracted from this repo and perseus-rapid-agent)
perseus-agent-core @ git+https://github.com/tcconnally/perseus-agent-core.git
