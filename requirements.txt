# Perseus Qwen Memory Agent — stdlib only, no runtime dependencies.
# The Qwen Cloud client uses urllib from the standard library;
# the Engram backend shells out to the engram binary.
#
# Dev/test:
#   pip install pytest

